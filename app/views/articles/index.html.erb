<h1>All Articles</h1>
<ul>
  <% @articles.each do |article| %>
    <li><%= link_to article.title, article_path(article) %> 
    <%= link_to "Delete", article_path(article), method: :delete %></li>
  <% end %>
</ul>
<%= link_to " + Create New", new_path %>
